/**
 * Company identity, in one place so the name cannot drift between the
 * sign-in page, the sidebar, the mobile header and the browser tab.
 */
export const BRAND = {
  name: "GAB Premium Ent",
  /** Sits under the name in the sidebar. */
  tagline: "Wholesale distribution",
  /** Two letters for the square mark. */
  initials: "GA",
} as const;
